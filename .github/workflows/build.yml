name: Build ClawChat APK

on:
  push:
    branches: [master, develop, feat/claw-integration]
  pull_request:
    branches: [master]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build Debug APK
        run: ./gradlew :TMessagesProj:compileBundleAfatDebugJavaWithJavac --warning-mode=none --no-daemon

      - name: Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: clawchat-debug
          path: TMessagesProj/build/outputs/apk/afat/debug/*.apk
          retention-days: 14
